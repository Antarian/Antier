# main docker compose file
version: '3.2'
services:
  antier:
    build:
      context: ./
      dockerfile: ./Dockerfiles/prod/Dockerfile
    tty: true
    volumes:
      - type: volume
        source: ./
        target: /usr/src/antier

